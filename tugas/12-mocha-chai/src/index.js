import { expect } from "chai";

// var sapa = "hai"

// expect(sapa).to.equal('hai')

describe ('LOGIN', function(){
    it ('Login dengan password salah', function(){
        expect(1).to.equal(1)
    })

    it ('login dengan username salah', function(){
        expect('panda').to.equal('beruang')
    })
})